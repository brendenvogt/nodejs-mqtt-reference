{
  "name": "server",
  "version": "1.0.0",
  "description": "reference server with mqtt",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brenden",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^2.18.8"
  }
}
